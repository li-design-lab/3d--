# 本地运行与交付说明

本说明对应当前本地版本，包含行政区下钻、地球、真实雷达与昌都 G214 桩点和桥梁。公网版本需单独发布，本地更新不会自动发布到公网。

## 本机日常使用（推荐）

双击桌面 **打开蓝色3D地图.command**，它会检查/恢复本地服务后用 Google Chrome 打开地图。

固定地址：<http://127.0.0.1:5174/?scene=g214>。全国地图：<http://127.0.0.1:5174/>。

macOS 用户级 LaunchAgent `local.blue-atlas.map` 在登录后自动启动，意外退出后自动恢复。服务运行副本位于 `~/Library/Application Support/Blue Atlas 3D/`，不依赖终端、Codex 会话或桌面源码目录持续存在。仅监听本机回环地址，未开放局域网访问；页面和数据不使用浏览器缓存。电脑关机、退出当前账户期间服务不运行，自启依赖 LaunchAgent 和 `/usr/local/bin/node` 保持可用。

修改项目后，在项目目录运行 `npm run local:install` 同步安装副本，再运行 `npm run local:status` 检查状态与版本。只修改源码不会更新已安装版本。

- 查看状态：`npm run local:status`
- 停止本次登录期间的服务：`npm run local:stop`
- 恢复服务：`npm run local:start`
- 移除登录自启：`node scripts/manage-local-service.mjs uninstall`（保留安装文件）
- 健康检查：<http://127.0.0.1:5174/_blue-atlas/health>
- 日志：`~/Library/Logs/Blue Atlas 3D/`

测试命令：`node --test scripts/local-server.test.mjs scripts/roads.test.mjs scripts/regions.test.mjs`。

## 其他电脑临时运行（已安装 Python 3）
在解压后的 blue-atlas 文件夹打开终端：

```bash
python3 -m http.server 8080 --bind 127.0.0.1 --directory dist/client
```

Windows 可改用 `py -m http.server 8080 --directory dist/client`。
浏览器访问 http://localhost:8080 。
不要双击 index.html 打开，本项目的模块与地图数据需要通过 HTTP 加载。

## 开发模式（Node.js 20 或更新版本）

```bash
npm ci
npm run dev
```

调试地址为 http://127.0.0.1:5176/，与日常使用的常驻服务 5174 分开。调试服务随终端关闭而停止，日常使用请走桌面入口。服务安装脚本使用 Node.js 24，已安装于本机。

## 文件说明

- dist/client/index.html：页面结构、控件图标和模块入口。
- dist/client/style.css：场景界面与悬浮控件样式。
- dist/client/app.js：三维地图、地球、图层、下钻和镜头交互。
- dist/client/globe-shell.js：球面蜂窝扫描罩与宽幅粒子光环。
- dist/client/assets/：地图 JSON、地球贴图、Three.js、雷达图层和光效依赖。
- package.json / package-lock.json / vite.config.js：本地开发环境。
- README.md：项目范围、数据来源和已知限制。

## 环境与数据

使用支持 WebGL 的现代浏览器并开启图形加速。项目仍使用 WebGL，没有切换为图片或视频展示。
已附带地图数据、纹理和运行依赖；边界从站点同源目录加载，缺少乡镇等下级边界时会明确提示，不会在浏览器中请求 DataV。
原行政场景的业务点位、告警和模拟热力数值为演示数据。公开公路专题使用独立生成的演示数据；本机安装时可从 `.private/road.json` 载入私有数据，私有文件不会进入源码和发布包。另附带真实 CREF 雷达反射率图层（2026-08-26 00:54–02:12）。浏览器资源在 `dist/client/assets/`，原始表格、原始点位 JSON 和 NetCDF 不会随项目发布。
可把 dist/client 文件夹部署到支持静态文件的 Web 服务器，无需后端构建。

本包不包含 node_modules、Git 历史或在线站点绑定；它们不影响本地运行。
第三方库和素材的来源参见 README.md 与 dist/assets/THREE-LICENSE.txt。

本机安装版本以健康检查中的 `revision` 文件内容摘要为准。
